const { horizontal, layerParent, fillParent } = require('csstips/lib')
const { em } = require('csx/lib')


module.exports = ({

} = {}) => {

  return [
    // fillParent,
    {
      $debugName: 'Link',
    }
  ]
}
