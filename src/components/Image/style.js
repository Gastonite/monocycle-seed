const { flex } = require('csstips/lib/flex')

module.exports = ({

} = {}) => {

  return [
    {
      $debugName: 'Image',
      display: 'block',
      // maxWidth: '230px',
      // maxHeight: '95px',
      
      height: 'auto',
      maxWidth: '100%',
      maxHeight: '100%',
    }
  ]
}
