const { horizontal, fillParent, flex, centerCenter, padding } = require('csstips/lib')


module.exports = ({

} = {}) => {

  return [
    // horizontal,
    {
      $debugName: 'LinkList',

    }
  ]
}
